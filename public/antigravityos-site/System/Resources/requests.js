/* Seed data for AntigravityOS profile-change requests.
   Runtime changes are stored in browser localStorage because a browser page
   cannot rewrite local JavaScript files on disk. */
window.ANTIGRAVITY_REQUESTS = [];
